let arr1 = [12, 123, 23324234];

// 배열객체를 생성하고 참조변수에 배열객체의 참조값을 대입하기
// 참조변수는 배열객체를 참조하고 있으며, 배열객체에 속성과 기능을 사용할 떄는
// 참조변수를 사용해야 한다.

// 0번째 부터 99번째 까지는 undefined로 되어있다.

console.log(f`a배열의 길이는 ${arr1.length}`);

const helloworld = () => {
  console.log("Hello, World!");
};
